# Dependencies
node_modules/
.bun/

# Build outputs
dist/
build/
out/
.next/
*.tsbuildinfo

# TypeScript compiled outputs in source directories
# (proper builds go to dist/, these are errant artifacts from IDE/improper builds)
packages/*/src/**/*.js
packages/*/src/**/*.d.ts
packages/*/src/**/*.js.map
packages/*/src/**/*.d.ts.map

# Temporary/scratch files
reproduce*.ts
scratch*.ts
temp*.ts
debug*.ts
repro_*.ts
test-*.ts
phage_illustrations/
a.out

# Generated database (root level only, web public db is tracked for deployment)
/phage.db
!packages/web/public/phage.db
# NEVER commit .db.gz - build process modifies DB, causing hash mismatches
packages/web/public/phage.db.gz

# Logs and coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*
*.log
coverage/

# Environment
.env
.env.*
!.env.example
!.env.*.example

# IDE/editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.fleet/
*.swp
*.swo
*~

# Lock files
*.lock
!bun.lockb

# Test artifacts
test-output/
test-results/
*.test-output

# UI inspection/audit scripts (debugging tools, not production code)
inspect-ui.ts
audit-ui.ts
packages/web/audit-ui.ts

# Temporary beads tracking data
.beads/beads.left.*
.beads/bd.sock.startlock
.beads/last-touched

# Web static database must be deployed with the site
# (kept small ~1.4MB; required for initial load)

# OS files
.DS_Store
.AppleDouble
.LSOverride
._*

# Installer lock
/tmp/phage-explorer-install.lock

# Rust / Wasm build artifacts
**/target/
**/.cargo/
**/wasm-pack.log
**/*.rmeta
**/*.rlib
packages/wasm-compute/target/
packages/wasm-compute/node_modules/
# Allow wasm-compute pkg/ to be tracked for Vercel deployment
!packages/wasm-compute/pkg/
.vercel
.env*.local

# bv (beads viewer) local config and caches
.bv/
